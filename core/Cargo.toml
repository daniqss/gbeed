[package]
name = "gbeed-core"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]
paste = "1.0.15"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "cpu_bench"
harness = false
